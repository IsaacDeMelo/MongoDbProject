<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>App</title>
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            background-color: rgb(255, 255, 255);
            min-height: 100vh;
            margin: 0;
        }

        .shadow {
            height: 100vh;
            width: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: white;
            background-color: rgba(0, 0, 0, 0.5);

        }

        .background {
            background-image: url("https://i.pinimg.com/736x/97/de/92/97de929b7462441703713db361cb29fb.jpg");
            background-size: cover;
            background-position: center;
        }

        .spam {
            position: absolute;
            top: 0;
            width: 100%;
            height: 100vh;
            display: flex;
            justify-content: end;
            align-items: end;
            flex-direction: column;
        }
        .options{
            display: flex;
            justify-content: space-around;
            width: 85%;
        }
        .btn {
            --border-color: linear-gradient(-45deg, #ffae00, #7e03aa, #00fffb);
            --border-width: 0.125em;
            --curve-size: 0.5em;
            --blur: 30px;
            --bg: #080312;
            --color: #afffff;
            color: var(--color);
            cursor: pointer;
            /* use position: relative; so that BG is only for .btn */
            position: relative;
            isolation: isolate;
            display: inline-grid;
            place-content: center;
            padding: 0.5em 1.5em;
            font-size: 17px;
            border: 0;
            box-shadow: 10px 10px 20px rgba(0, 0, 0, 0.6);
            clip-path: polygon(
                    /* Top-left */
                    0% var(--curve-size),
                    var(--curve-size) 0,
                    /* top-right */
                    100% 0,
                    100% calc(100% - var(--curve-size)),
                    /* bottom-right 1 */
                    calc(100% - var(--curve-size)) 100%,
                    /* bottom-right 2 */
                    0 100%);
            transition: color 250ms;
        }
        .btn::after,
        .btn::before {
            content: "";
            position: absolute;
            inset: 0;
        }
        .btn::before {
            background: var(--border-color);
            background-size: 300% 300%;
            animation: move-bg7234 5s ease infinite;
            z-index: -2;
        }
        @keyframes move-bg7234 {
            0% {
                background-position: 31% 0%;
            }

            50% {
                background-position: 70% 100%;
            }

            100% {
                background-position: 31% 0%;
            }
        }
        .btn::after {
            background: var(--bg);
            z-index: -1;
            clip-path: polygon(
                    /* Top-left */
                    var(--border-width) calc(var(--curve-size) + var(--border-width) * 0.5),
                    calc(var(--curve-size) + var(--border-width) * 0.5) var(--border-width),
                    /* top-right */
                    calc(100% - var(--border-width)) var(--border-width),
                    calc(100% - var(--border-width)) calc(100% - calc(var(--curve-size) + var(--border-width) * 0.5)),
                    /* bottom-right 1 */
                    calc(100% - calc(var(--curve-size) + var(--border-width) * 0.5)) calc(100% - var(--border-width)),
                    /* bottom-right 2 */
                    var(--border-width) calc(100% - var(--border-width)));
            transition: clip-path 500ms;
        }
        .btn:where(:hover, :focus)::after {
            clip-path: polygon(
                    /* Top-left */
                    calc(100% - var(--border-width)) calc(100% - calc(var(--curve-size) + var(--border-width) * 0.5)),
                    calc(100% - var(--border-width)) var(--border-width),
                    /* top-right */
                    calc(100% - var(--border-width)) var(--border-width),
                    calc(100% - var(--border-width)) calc(100% - calc(var(--curve-size) + var(--border-width) * 0.5)),
                    /* bottom-right 1 */
                    calc(100% - calc(var(--curve-size) + var(--border-width) * 0.5)) calc(100% - var(--border-width)),
                    /* bottom-right 2 */
                    calc(100% - calc(var(--curve-size) + var(--border-width) * 0.5)) calc(100% - var(--border-width)));
            transition: 200ms;
        }
        .btn:where(:hover, :focus) {
            color: #fff;
        }
    </style>
</head>

<body>
    <div class="background">
        <div class="shadow">
            <img src="https://pa1.aminoapps.com/8162/479df0f08821d5e023cfb189b9398dddc240824cr1-256-256_00.gif"
                width="100">
            <h1 style="font-size: 50px">OtherWorld's Gate</h1>

        </div>
    </div>
    <div class="spam" id="spam" style="display: none;">
        <div class="btn">
            <h3 id="balontext">Ei, é sua primeira vez aqui?</h3>
        </div>
        <div class="options">
            <button class="btn" id="firstTrue">Sim</button>
            <button class="btn" id="firstFalse">Não</button>
        </div>
        <img id="safira" src="https://media.tenor.com/o64xmWOq_KAAAAAi/neuvillette-genshin-impact.gif" style="transition: 3s all;">
    </div>

    <script>
        // Verifica se já houve uma visita anteriormente
        if (!localStorage.getItem('visited')) {
            // Se não houver, exibe a seção de spam
            document.getElementById('spam').style.display = 'flex';
        }

        document.getElementById("firstTrue").addEventListener('click', ()=> {
            document.getElementById("safira").style.transition = "3s all linear";
            document.getElementById("safira").src = "https://media.tenor.com/RAluSq5ZWN8AAAAi/neuvillette-genshin-impact.gif";
            document.getElementById("balontext").innerHTML = "Isso é problema seu! Em breve meu programador me dará a função de te apresentar o site, mas por enquanto, se vira."
            Array.from(document.getElementsByTagName("button")).forEach(element => {
                element.style.display = "none";
            });
            setTimeout(() => {
                document.getElementById("spam").style.display = "none";
            }, 7000);
            
            // Marca o site como visitado
            localStorage.setItem('visited', true);
        });

        document.getElementById("firstFalse").addEventListener('click', ()=> {
            document.getElementById("safira").style.transition = "3s all linear";
            document.getElementById("safira").src = "https://media.tenor.com/wmEx6bp6SocAAAAi/neuvillette-genshin-impact.gif";
            document.getElementById("balontext").innerHTML = "Seja bem vindo novamente!"
            Array.from(document.getElementsByTagName("button")).forEach(element => {
                element.style.display = "none";
            });
            setTimeout(() => {
                document.getElementById("spam").style.display = "none";
            }, 3000);

            // Marca o site como visitado
            localStorage.setItem('visited', true);
        });

    </script>

</body>

</html>